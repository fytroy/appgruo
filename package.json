{
  "name": "appgruo",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "firebase": "^10.12.2", // Ensure you have a recent version
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-scripts": "5.0.1" // Or vite for vite projects
  },
  "scripts": {
    "start": "react-scripts start", // Or "vite" for vite projects
    "build": "react-scripts build", // Or "vite build" for vite projects
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
